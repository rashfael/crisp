@import '_ui'

.list
	flex 1 0
	display flex
	flex-direction column
	max-width 1200px
	margin 0 auto
	.toolbar
		card()
		display flex
		justify-content space-between

	table
		card()
		table()
		tr
			cursor pointer

			&:hover
				background-color clr-grey-200
		td, th
			height 32px
